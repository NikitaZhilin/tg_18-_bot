# Повторный аудит всех карточек

Дата проверки: 28 июля 2026 года.

## Охват

Проверены оба встроенных каталога:

- `content/cards.csv`: 182 карточки;
- `content/restricted_cards.csv`: 20 карточек;
- всего: 202 карточки;
- активных и одобренных: 199;
- отключенных пользовательских черновиков: 3.

Проверка выполнена для всех заданий, вопросов, желаний, поз и игровых
штрафов. Для каждой карточки сверялись название, исходное положение,
роли, конкретное действие, участок тела, последовательность, переходы,
таймер, реквизит, завершение и связь с типом карточки.

В первоначальном каталоге было 218 карточек. На первичном проходе были
изменены текст или таймер у 199 карточек. Оставшиеся на тот момент 19
карточек прочитаны вручную и подтверждены без изменения; в это число входят
уже конкретные карточки и три намеренно отключенных черновика.

## Найденные системные проблемы

До исправлений строгий аудитор нашел 118 дефектов:

- 61 размытая или контекстная ссылка;
- 34 таймера без понятного действия или длительности;
- 23 позы, где раздел `Действие` не повторял конкретное назначение позы.

Главной причиной были ссылки вида `из блока «Контекст»`,
`из раздела «Порядок»`, `выберите точную последовательность` и
`названное действие`. Игроку приходилось самостоятельно сопоставлять
несколько абзацев. В части поз назначение содержало проникновение,
куннилингус или ручную стимуляцию, а исполняемый раздел описывал только
темп или положение рук.

## Выполненные исправления

- Во всех 54 карточках со ссылкой на блок `Контекст` точное положение и
  действие перенесены непосредственно в раздел `Порядок`.
- Стандартные завершения больше не отсылают к другому разделу: теперь
  они прямо требуют полностью остановиться, убрать руки или реквизит и
  разомкнуть контакт.
- Во всех 36 оставшихся позах удален служебный раздел `Назначение`, который
  дословно повторял начало раздела `Действие`. Теперь конкретная активность,
  положение тела, длительность и завершение описаны без повторяющегося абзаца.
- Исправлены неявные переходы между оральными ласками и проникновением:
  новый вид действия начинается только после полной остановки, отдельного
  вопроса и нового ответа `да`.
- Переписаны карточки, где положение тела не совпадало с командами или
  последующим действием.
- Разговорные карточки больше не требуют угадывать содержание будущих
  карточек. Их результат относится к текущей игре и сформулирован явно.
- Два неясных желания на будущее заменены на купоны с полностью описанными
  вариантами, правилами сохранения, последующего использования и завершения.
- Таймеры удалены из чистых обсуждений и итоговых карточек. Для коротких
  действий установлены фактические значения 30, 60, 90 или 120 секунд.
- Карточки со случайным реквизитом прямо сообщают, что предмет выбирает
  бот, а игрок выбирает только разрешенный способ, участок и интенсивность.
- Карточки с фиксированным реквизитом и переменной длительностью приведены
  к одному значению, совпадающему с таймером Telegram.
- Три высокорисковых пользовательских шаблона остаются отключенными
  черновиками и не участвуют в рулетке.

## Автоматический контроль

Добавлен `scripts/audit_card_content.py`. Он проверяет:

- обязательную структуру для каждого типа карточки;
- запрещенные размытые формулировки и ссылки на другие разделы;
- соответствие таймера тексту;
- наличие конкретного действия во всех позах;
- отсутствие повторяющихся названий и текстов.

Аудитор подключен к `tests/test_content_clarity.py` и выполняется вместе с
общими тестами проекта.

Результат после исправлений:

| Проверка | Ошибок |
|---|---:|
| Неполная структура | 0 |
| Размытые ссылки и формулировки | 0 |
| Несогласованные таймеры | 0 |
| Неполное действие в позах | 0 |
| Дубли названий или текстов | 0 |
| Предупреждения импорта | 0 |

Оба CSV успешно проходят сухой импорт: 182 и 20 карточек соответственно.

## Полная семантическая проверка

После первичной очистки шести дублей в каталоге осталось 212 карточек.
По требованию владельца все они повторно проверены тем же способом:

- восемь самостоятельных карточек переписаны с конкретным порядком действий;
- еще десять повторяющихся или служебных карточек удалены из встроенного
  контента; вместе с первой очисткой удалено шестнадцать;
- у 50 заданий удален дословно повторявшийся абзац `Контекст`;
- у 38 исходных поз удален повтор разделов `Назначение` и `Действие`;
- у всех карточек удалены ссылки вида `выполняйте последовательность` и
  `после пунктов раздела Порядок`;
- исправленные карточки возвращены в статус `approved`;
- открытые отметки `На доработку` для этой партии закрываются миграцией;
- удаленные карточки архивируются и исключаются из рулетки, поиска и экспорта.

Удалены: `task_l1_010`, `task_l1_011`, `task_l1_018`, `task_l1_020`,
`task_l1_021`, `task_l1_023`, `task_l2_004`, `task_l4_hard_004`,
`task_l4_hard_014`, `task_l4_medium_008`, `task_l4_medium_016`,
`pose_ks_031`, `pose_ks_035`, `penalty_seed_002`,
`restricted_l4_hard_urethral_002`, `restricted_q_002`.

| Удаленная карточка | Причина и сохраненная замена |
|---|---|
| `task_l1_010` | Повторяла выбор сигнала паузы; сохранена более полная `question_seed_003`. |
| `task_l1_018` | Повторяла три вопроса о текущих желаниях, границах и темпе; сохранена `task_l1_004`. |
| `task_l1_020` | Дублировала обязательную подготовку и подтверждение в начале сессии, уже реализованные самим ботом. |
| `task_l1_021` | Повторяла настройку света и звука из `question_seed_001` и купона `desire_seed_001`. |
| `penalty_seed_002` | Повторяла обычную паузу, воду и выбор темпа; это уже доступно через остановку сессии и отдельные карточки темпа. |
| `restricted_q_002` | Повторяла выбор разрешенных команд, который полностью выполняют `restricted_t_003` и `restricted_t_004`. |
| `task_l1_011` | Повторяла фиксацию и пересказ границы из `task_l1_005`. |
| `task_l1_023` | Повторяла массаж плеч, кистей или спины из `task_l1_007`. |
| `task_l2_004` | Повторяла запрос разрешения на один мягкий жест из `task_l1_016`. |
| `task_l4_hard_004` | Дословно повторяла команды и действие из `task_l4_hard_001`. |
| `task_l4_hard_014` | Повторяла фиксированную последовательность трех действий из `task_l3_hard_002`. |
| `task_l4_medium_008` | Повторяла одну мягкую просьбу из `task_l4_light_005`. |
| `task_l4_medium_016` | Повторяла границу до конца игры из `task_l3_medium_016`. |
| `pose_ks_031` | Повторяла миссионерскую позу с подушкой под тазом из `pose_ks_010`. |
| `pose_ks_035` | Повторяла положение, ручную стимуляцию и команды из `pose_ks_023`. |
| `restricted_l4_hard_urethral_002` | Повторяла проверку допуска из `restricted_l4_hard_urethral_001`. |

Переписаны: `task_l1_013`, `task_l1_017`, `task_l1_019`,
`task_l4_light_015`, `question_seed_003`, `question_seed_004`,
`desire_seed_001`, `desire_seed_004`.

## Правило дальнейшего редактирования

Карточка должна быть выполнима без догадок и без поиска смысла в другом
абзаце. Раздел с действием обязан самостоятельно отвечать на вопросы:

1. Кто и в каком положении находится.
2. Что именно делает каждый партнер.
3. Какой участок тела или предмет используется.
4. Сколько длится действие и когда включается таймер.
5. Когда нужна полная остановка и новое согласие.
6. Как карточка завершается.

Для высокорисковой практики допустима карточка проверки готовности или
подготовки без пошаговой медицинской техники.
